<Window x:Class="MyProject.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Password" Height="260" Width="420">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="New Password" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="NewPasswordInput" Grid.Row="0" Grid.Column="1" Margin="6,4"/>

        <TextBlock Text="Confirm Password" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="ConfirmPasswordInput" Grid.Row="1" Grid.Column="1" Margin="6,4"/>

        <TextBlock x:Name="ValidationText" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Foreground="Red" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Save" Width="80" Margin="6"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Margin="6"/>
        </StackPanel>
    </Grid>
</Window>