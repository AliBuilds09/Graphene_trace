<Window x:Class="MyProject.Views.CreateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create User" Height="340" Width="420">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Username" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="UsernameInput" Grid.Row="0" Grid.Column="1" Margin="6,4"/>

        <TextBlock Text="Temp Password" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordInput" Grid.Row="1" Grid.Column="1" Margin="6,4"/>
        <Button x:Name="GeneratePasswordButton" Grid.Row="1" Grid.Column="2" Content="Generate" Margin="6,4"/>

        <TextBlock Text="Role" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="RoleCombo" Grid.Row="2" Grid.Column="1" Margin="6,4" Width="160">
            <ComboBoxItem Content="Patient"/>
            <ComboBoxItem Content="Clinician"/>
            <ComboBoxItem Content="Admin"/>
        </ComboBox>

        <TextBlock Text="Assign (optional)" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="AssignInput" Grid.Row="3" Grid.Column="1" Margin="6,4" ToolTip="Assign clinician or patients (comma-separated)"/>

        <TextBlock x:Name="ValidationText" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Foreground="DarkRed"/>

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CreateButton" Content="Create" Width="90" Margin="6"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="90" Margin="6"/>
        </StackPanel>
    </Grid>
</Window>